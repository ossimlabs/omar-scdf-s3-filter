spring:
  application:
    name: omar-scdf-s3-filter
  cloud:
    config:
      enabled: true
      uri: ${SPRING_CONFIG_URI:http://localhost:8888/scdf-config-server}
    discovery:
      enabled: true
